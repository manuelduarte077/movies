name: movies
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
    sdk: '>=2.17.0 <3.0.0'

dependencies:
    card_swiper: ^2.0.4
    cupertino_icons: ^1.0.2
    flutter:
        sdk: flutter
    google_fonts: ^2.3.2
    http: ^0.13.4
    provider: ^6.0.2

dev_dependencies:
    flutter_lints: ^2.0.0
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true

    assets:
        - assets/
